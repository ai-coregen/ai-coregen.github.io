---
import SectionHeading from "./ui/SectionHeading.astro";
import AccordionItem from "./ui/AccordionItem.astro";
import JaText from "./ui/JaText.astro";
import { faq } from "../data/content";
---

<section class="bg-white py-20 lg:py-32" id="faq">
  <div class="max-w-[1100px] mx-auto px-6 lg:px-8">
    <SectionHeading eyebrow={faq.eyebrow}>
      <JaText tag="span" text={faq.heading} class="" />
    </SectionHeading>

    <div class="max-w-[800px] mx-auto" data-animate>
      {faq.items.map((item) => (
        <AccordionItem question={item.question} answer={item.answer} />
      ))}
    </div>
  </div>
</section>

<script>
  import "../scripts/accordion.ts";
</script>
